# Ticket type schema for "task"
#
# Mirrors the core implementation workflow while preserving a dedicated task
# type. Edit or copy this file to define custom ticket types; load custom
# schemas with `ticket --schema-dir <dir>`.

type_id = "task"

states = [
    "open",
    "planned",
    "in-implementation",
    "in-review",
    "on-hold",
    "done",
    "cancelled",
]

# States that must appear in a ticket's history before it can reach a terminal
# state (done). This enforces that every ticket passes through review.
required_states = ["in-review"]

# ── State machine transitions ──────────────────────────────────────────────────

[[transitions]]
from = "open"
to = "planned"

[[transitions]]
from = "open"
to = "cancelled"

[[transitions]]
from = "planned"
to = "in-implementation"

[[transitions]]
from = "planned"
to = "cancelled"

[[transitions]]
from = "planned"
to = "on-hold"

[[transitions]]
from = "in-implementation"
to = "in-review"

[[transitions]]
from = "in-implementation"
to = "cancelled"

[[transitions]]
from = "in-implementation"
to = "on-hold"

[[transitions]]
from = "in-review"
to = "done"

[[transitions]]
from = "in-review"
to = "cancelled"

[[transitions]]
from = "in-review"
to = "on-hold"

[[transitions]]
from = "on-hold"
to = "cancelled"

# ── Field definitions ──────────────────────────────────────────────────────────
# Required fields must be present in every manifest of this type.
# Optional fields are indexed when present.

[fields.title]
field_type = "string"
required = true

[fields.type]
field_type = "string"
required = true

[fields.state]
field_type = "string"
required = false

[fields.component]
field_type = "string"
required = false

[fields.risk_level]
field_type = "string"
required = false

[fields.acceptance_criteria]
field_type = "string"
required = false

[fields.validation_plan]
field_type = "string"
required = false

[fields.validation_status]
field_type = "string"
required = false

[fields.validator_id]
field_type = "string"
required = false

[fields.release_target]
field_type = "string"
required = false

[fields.release_version]
field_type = "string"
required = false

[fields.effort]
field_type = "string"
required = false

[fields.bootstrap_blocker]
field_type = "boolean"
required = false

[fields.rollout_stage]
field_type = "string"
required = false

[fields.doc_category]
field_type = "string"
required = false

[fields.tags]
field_type = "string"
required = false

[fields.workflow_stage]
field_type = "string"
required = false

[fields.priority]
field_type = "string"
required = false

[fields.source_agent_files]
field_type = "string"
required = false

[fields.phase]
field_type = "string"
required = false

# ── Edge kind rules ────────────────────────────────────────────────────────────

[edge_rules.depends_on]
directed = true
acyclic_enforced = true

[edge_rules.linked]
directed = false
acyclic_enforced = false
